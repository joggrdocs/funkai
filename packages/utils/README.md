# @funkai/utils

Internal utilities for the funkai framework. Not published to npm — bundled into consuming packages at build time.

## Install

```bash
# In a workspace package's package.json:
"devDependencies": {
  "@funkai/utils": "workspace:*"
}
```

## Docs

See [`docs/`](./docs/) for reference documentation.
